option(GLAD_DEBUG "Sets whether glad is in debug mode" ON)
add_library(glad gl.c)
target_include_directories(glad PUBLIC include)
if(${GLAD_DEBUG})
    target_compile_definitions(glad PUBLIC GLAD_DEBUG)
    message(STATUS "Glad debug mode in on")
else()
    message(STATUS "Glad debug mode in off")
endif()